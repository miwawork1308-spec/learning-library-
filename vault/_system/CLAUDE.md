# CLAUDE.md

Claude Code向けの指示。基本方針は `AGENTS.md` と共通。

## 役割分担
- ユーザーは上のメイン部分（番号付きフォルダ）だけを触る。
- AIへの指示・運用ルールはこの `_system/` 配下に置く。

## 作業の原則
- 元素材（`08_Clipping`）は書き換えない。
- 外部記事の整理版は `07_Research` に出す。
- 自分のメモ（`01_Inbox`）と借り物（`08_Clipping`）を混ぜない。
- 迷ったら `99_System/Vault運用ガイド.md` を参照する。
